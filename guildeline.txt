environment: 
virtualenv env
PS E:\Documents\CerebralStroke_DataAnalysis_ML_Django> env/scripts/activate
(env) PS E:\Documents\CerebralStroke_DataAnalysis_ML_Django> deactivate
pip install django

make project: django-admin startproject cerebral_stroke ./

make app: django-admin startapp base

need to tell project about base: base.apps.BaseConfig

need to add the cerebral_stroke/tmeplates to the templates of settings.py

main pages fo the project:
login, 
intro to cerebral stoke
check risk->form page
powerbi

main models:
users(AbstractUser): username, email, password, 
predictions: user(User object, many to one reation), 
gender(male or female only), age(integer), height(float), weight(float), smoking_status(never, formerly, smokes), risk(0 or 1)
register models to admin

styles:
created static dir:
    app
    project: templates/main.html
    static: styles/main.css
changed settings:   'django.template.context_processors.static'  STATICFILES_DIRS = [ 
    BASE_DIR / "static"]
load static in main.html and link the css file using static {% static 'styles/main.css' %}
linked bootstrap

prediction:
submit form, get data from post, transformation of data(), model inference, prediction, display results and then store in predictions with the user name


